# --- Detección de Placas ---
ultralytics>=8.0.0           # Framework YOLO (detección de objetos)
opencv-python>=4.5.5         # Procesamiento de imágenes y video (RTSP, webcam)
imutils>=0.5.4               # Utilidades para manipular frames y rotaciones
numpy>=1.20.0                # Base numérica para cálculos de imagen y tensor

# --- OCR (Reconocimiento de texto en placas) ---
paddleocr>=2.6.0             # Librería OCR para extraer texto de las placas
paddlepaddle>=3.0.0          # Backend necesario para PaddleOCR (CPU version). Relaxed pin: use latest compatible 3.x on Windows.
# NOTE: If you need GPU support or a specific build for your hardware, follow the official install
# instructions: https://www.paddlepaddle.org.cn/install/quick (choose the correct wheel for Windows)

# --- Interfaz Web / Dashboard ---
flask>=2.0.0                 # Framework web para mostrar resultados
flask-cors>=3.0.10           # Permite acceso desde otros orígenes al dashboard
requests>=2.28.0             # Para enviar datos del reconocimiento a APIs o DBs

# --- Utilidades adicionales ---
Pillow>=9.0.0                # Manejo de imágenes (capturas, conversiones)
tqdm>=4.65.0                 # Barra de progreso para procesamiento en lotes
psutil>=5.9.0                # Monitoreo de CPU/GPU para rendimiento
ffmpeg-python>=0.2.0         # Control de streams RTSP, grabación de video

# --- Compatibilidad con GPU AMD (opcional) ---
# Permite ejecutar PyTorch en GPU AMD mediante DirectML (en Windows).
# Este paquete no está disponible en todas las plataformas/versions de Python y puede
# provocar fallos al instalar todo el requirements. Lo dejamos como opcional —
# si necesitas DirectML instala manualmente siguiendo las instrucciones de Microsoft:
# https://github.com/microsoft/DirectML
# Para evitar errores de instalación descomenta la línea abajo solo si tu entorno
# soporta torch-directml.
# torch-directml>=0.2.0        # Recomendado si usas Radeon Graphics (OPTIONAL)

# --- Visualización opcional ---
matplotlib>=3.5.0            # Solo si quieres graficar estadísticas o métricas
